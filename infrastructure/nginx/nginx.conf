events {
    worker_connections 1024;
}

http {
    upstream gateway {
        server gateway:3000;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://gateway;
        }
    }
}
